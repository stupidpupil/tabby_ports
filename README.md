
MacPorts can sync git repos by itself if they're added as file:// to sources.conf

Install `tabby_build_tools` to keep common build dependencies installed (including current compilers and languages frequently used by build processes like Go, Python, Perl).
