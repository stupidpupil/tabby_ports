PortSystem          1.0

name                tabby_ruby
version             2026-08-17
revision            0
categories          ruby
supported_archs     noarch
license             permissive
maintainers         nomaintainer
description         Meta-port pinning stuff I want for ruby
long_description     ${description}

homepage            https://github.com/stupidpupil/tabby_ports
distfiles

depends_run-append  port:rbenv \
                    port:ruby-build \
                    port:tabby_other_lang_deps

use_configure       no
build {}
destroot {
    xinstall -d ${destroot}${prefix}/share/doc/${name}
    touch ${destroot}${prefix}/share/doc/${name}/${name}
}
