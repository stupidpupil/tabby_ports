PortSystem          1.0

name                tabby_compilers
version             2026-08-13
revision            0
categories          devel
supported_archs     noarch
license             permissive
maintainers         nomaintainer
description         Meta-port pinning common build-time compilers as non-leaves
long_description     ${description}

homepage            https://github.com/stupidpupil/tabby_ports
distfiles

depends_run-append \
                    port:gcc15 \
                    port:clang-22


use_configure       no
build {}
destroot {
    xinstall -d ${destroot}${prefix}/share/doc/${name}
    touch ${destroot}${prefix}/share/doc/${name}/${name}
}
