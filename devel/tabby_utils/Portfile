PortSystem          1.0

name                tabby_utils
version             2026-08-17
revision            0
categories          devel
supported_archs     noarch
license             permissive
maintainers         nomaintainer
description         Meta-port pinning tools I expect to have available on any system
long_description     ${description}

homepage            https://github.com/stupidpupil/tabby_ports
distfiles

depends_run-append  port:libqalculate \
                    port:mpv \
                    port:htop \
                    port:watch

use_configure       no
build {}
destroot {
    xinstall -d ${destroot}${prefix}/share/doc/${name}
    touch ${destroot}${prefix}/share/doc/${name}/${name}
}
